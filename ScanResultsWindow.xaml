<Window x:Class="NAZARICK_Protocol.ScanResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="N.A.Z.A.R.I.C.K. Protocol - Scan Results" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Standard Button Style -->
        <Style x:Key="StandardButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Margin="30,20">
                <TextBlock Text="Scan Results" FontSize="28" FontWeight="Bold" 
                          Foreground="#333333" Margin="0,0,0,5"/>
                <TextBlock x:Name="SummaryText" Text="Loading results..." FontSize="14" 
                          Foreground="#666666"/>
            </StackPanel>
        </Border>

        <!-- Results Section -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListView x:Name="ResultsListView" BorderThickness="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="File Path" Width="400" DisplayMemberBinding="{Binding FilePath}"/>
                            <GridViewColumn Header="Status" Width="100" DisplayMemberBinding="{Binding StatusText}"/>
                            <GridViewColumn Header="Rules Matched" Width="120" DisplayMemberBinding="{Binding MatchedRulesCount}"/>
                            <GridViewColumn Header="Detected Threats" Width="350" DisplayMemberBinding="{Binding ThreatsList}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </ScrollViewer>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="30,15">
                <Button x:Name="CloseButton" Content="Close" Width="100" Height="35" 
                       Background="#0078D4" Foreground="White" BorderThickness="0"
                       Style="{StaticResource StandardButtonStyle}"
                       Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>